Ниже пары пунктов из документов «после» реорганизации, похожие по тексту. Для каждой пары определи, закрепляют ли они одну и ту же функцию за разными исполнителями.

- same — та же функция, обязанность или право (та же суть действия и объект), даже если формулировки различаются или названы разные цели;
- overlap — функции явно пересекаются: одна включает другую, либо это одно действие с разной областью применения;
- none — разные функции, сходство только в словах.

Верни ровно один ответ для каждого входного pair_id, без пропусков и повторений.

Схема ответа:
{"results":[{"pair_id":1,"relation":"same|overlap|none","confidence":0.8}]}

<document>
{{pairs}}
</document>
